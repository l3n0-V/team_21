# Mobile App Environment Configuration

# For testing on physical devices (iPhone, Android phone):
# Set this to your computer's local IP address on the same WiFi network
# To find your IP:
#   - Windows: ipconfig (look for IPv4 Address)
#   - Mac/Linux: ifconfig (look for inet)
# Example: EXPO_PUBLIC_API_HOST=192.168.1.100
# EXPO_PUBLIC_API_HOST=

# API Port (default: 5000)
# Change this if Flask is running on a different port
# EXPO_PUBLIC_API_PORT=5000

# API Protocol (default: http)
# Use https for production deployments
# EXPO_PUBLIC_API_PROTOCOL=http

# Note: Leave EXPO_PUBLIC_API_HOST commented for emulators/simulators
# (they automatically use localhost or 10.0.2.2)
